enum AppBarBehavior { normal, pinned, floating, snapping }
//choice categories
enum CategoryItem { Religion, Age, Height, Education, Jobs, Race }

enum PaymentSender {
  friendship,
  trade,
  job,
}
